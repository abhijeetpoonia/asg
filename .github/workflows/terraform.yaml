name: Terraform Deployment
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
    secrets:
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
jobs:
  terraform-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Remove default repo (dope)
        run: rm -rf ${{ github.workspace }}/*

      - name: Checkout terraform repository
        uses: actions/checkout@v3
        with:
           repository: abhijeetpoonia/asg
           ref: main
           # Ensure that you check it out directly to the root workspace
           # No need for 'path' here, remove the path: asg entry
           
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Terraform Init
        run: terraform init
        working-directory: environments/dev  # Path relative to the root of the repo

      - name: Terraform Validate
        run: terraform validate
        working-directory: environments/dev  # Path relative to the root of the repo

      - name: Terraform Plan
        run: terraform plan
        working-directory: environments/dev  # Path relative to the root of the repo

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main'
        run: terraform apply -auto-approve
        working-directory: environments/dev  # Path relative to the root of the repo
